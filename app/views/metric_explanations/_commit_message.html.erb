<strong>What's this?</strong>
<p>This metric shows how well you use the commit template and a counting of commit types.</p>
<p>We recommend you use a simple commit template where the <a href="https://github.com/commitizen/conventional-commit-types" target="_blank">commit type</a> is specified with <code>[TYPE]</code> at the very beginning of a commit message.</p>
<p>An example:<code>[feat] Implement user login feature.</code></p>
<p>Data is collected from <a href="https://developer.github.com/v3/repos/commits/" target="_blank">GitHub API</a>.</p>

<strong>What should I expect?</strong>
<p>We recommend to use the commit template. So you should expect no "untyped" commit.</p>